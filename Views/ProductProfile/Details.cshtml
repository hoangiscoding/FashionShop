@model FashionShop.Models.ProductProfile

@{
    string currentModel = Helper.GetTypeName(Model.GetType().ToString());
    ViewData["Title"] = currentModel + " - Details";
}

<h1>@currentModel - Details</h1>

<div asp-validation-summary="ModelOnly" class="text-danger"></div>
<div>    
    <hr />

    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.TenHoSoSP)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.TenHoSoSP)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.MoTa)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.MoTa)
        </dd>
    </dl>
</div>
<div class="btn-group">
    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.MaHoSoSP">Edit</a>
    <a class="btn btn-secondary" asp-action="index"
       asp-route-pg="@TempData["CurrentPage"]">Back</a>    
</div>
